module CybFFmpegC {
    header "include/cyb_ffmpeg.h"
    // No `link` directive: libcyb_ffmpeg_core.a is supplied by the
    // `CybFFmpegCore` binaryTarget (library-only XCFramework) and linked by
    // SwiftPM via its full path.
    export *
}
